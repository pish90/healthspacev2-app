import { HealthProfessional } from './health-professional';

describe('HealthProfessional', () => {
  it('should create an instance', () => {
    expect(new HealthProfessional()).toBeTruthy();
  });
});
